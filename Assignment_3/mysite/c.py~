#from dashboard.models import node, edges
f = open("log-graph.out", "rb")
fw = open("output.out", "rw+")
while True:
	x = f.readline()
	if not x: break
	x = x.split(" ")
	if x[1] == 'edge':
		a = x[2].split("-")
                source = a[0];
                target = a[1].split("\r\n")[0]
		fw.write(source + " " + target + "\n")	

f.close()
fw.close()
